## Практическая работа № 1
выполнила студентка 2 курса группы ЭФБО-06-25 Меньшикова Виктория Даниловна, вариант 8 (16 номер по списку)

Тема: Подготовка окружения и первые программы на JavaScript
Цель работы — подготовить рабочее окружение и освоить базовый цикл разработки программы на JavaScript: написать код, запустить его, проверить результат, исправить ошибку и сохранить изменения в Git.

3. Инструменты и предварительная подготовка
Установила  Node.js
￼
3.2. Проверка установки
Откройте терминал редактора или отдельное приложение терминала. Выполните команды по одной:
node --version
npm --version
git --version
￼
5. Подготовка проекта

Шаг 1. Создайте отдельную папку и репозиторий
￼
Задайте автора коммитов для этого репозитория, заменив пример своими данными:
￼
Создайте через редактор следующие папки и файлы:

￼
Шаг 4. Сохраните начальный этап

После создания структуры и начального содержимого выполните:
￼
Запуск через Node.js. Из корня репозитория выполните:
￼
Запуск в браузере. Откройте practice-01/index.html, затем инструменты разработчика через меню браузера. Перейдите во вкладку Console / Консоль. Сравните вывод с терминалом.
￼
Задание 2. Исследование типов и преобразований

В practice-01/js/types.js начните со строки "use strict";. Для каждого выражения сначала предскажите значение и его тип, затем проверьте результат через console.log().
№	Выражение
1	"8" + 2
2	"8" - 2
3	Number("8") + 2
4	"12" > "3"
5	12 === "12"
6	Number("")
7	Number("text")
8	Boolean("false")
9	typeof null
10	typeof NaN
Через node practice-01/js/types.js
￼
Через браузер
￼
Задание 3. Сводка выполнения задач
￼
￼
Задание 4
￼
￼

Задание 5
Порядок отладки:
1. Сохраните исходный вариант и запишите его фактический вывод в отчёт. Отсутствие исключения ещё не доказывает правильность вычисления.
￼
1. Подключите в HTML ./js/debug.js, откройте DevTools и вкладку Sources / Источники.
2. Откройте debug.js и поставьте точку останова на строке вычисления completedTotal, щёлкнув по номеру строки.
3. Перезагрузите страницу. До выполнения этой строки исследуйте значения и типы completedText и additionalText через Scope / Область видимости либо Console.
￼
1. Выполните один шаг Step over / Шаг с обходом. Теперь проверьте значение и тип completedTotal.
2. Поставьте точку останова внутри цикла. Проследите изменение taskNumber и controlSum, а также момент прекращения цикла
￼
1. Исправьте преобразование исходных строк и границу цикла. Не заменяйте вычисления константами 5, 3 и 10.
	8	Повторно запустите программу в браузере и Node.js. Удалите активные точки останова, мешающие обычной демонстрации.
￼
￼
В отчёт добавьте одну иллюстрацию остановленной программы, например screenshots/debug-breakpoint.png, и таблицу:
Ошибка	Что наблюдалось	Причина	Исправление	Результат после исправления	Исправление	Результат после исправления
Обработка количества задач	console.log("Выполнено:", completedTotal) выводил "32" вместо 5. Строка remainingTasks = plannedText - completedTotal давала -24 вместо 3.	Оператор + со строковыми операндами выполняет конкатенацию, а не сложение. completedText и additionalText имели тип string ("3" и "2"), поэтому "3" + "2" склеилось в "32". Дальше "8" - "32" привело обе стороны к числам и дало -24.	Явное преобразование строк в числа: Number(completedText) + Number(additionalText). Аналогично для remainingTasks — через Number(plannedText).	Выполнено: 5, Осталось: 3. Значения совпадают с ожидаемыми, типы — number.	Заполнить	Заполнить
Граница цикла	controlSum после цикла равнялась 6 (1 + 2 + 3), а ожидалось 10. Последняя итерация с taskNumber = 4 не выполнялась.	Условие taskNumber < 4 останавливает цикл, когда taskNumber становится равным 4, то есть само значение 4 в тело цикла не попадает. Нужна была нестрогая граница.			Заполнить	Заполнить
Дополнительное здание вариант А
"use strict";

const plannedText = " 14 ";
const completedText = "4";

function parseTaskInput(value, name) {
  if (typeof value !== "string") {
    return { ok: false, error: `Ошибка: ${name} должен быть строкой` };
  }
  const trimmed = value.trim();

  if (trimmed === "") {
    return { ok: false, error: `Ошибка: ${name} не может быть пустым` };
  }

  const num = Number(trimmed);

  if (Number.isNaN(num)) {
    return { ok: false, error: `Ошибка: ${name} содержит не число: "${value}"` };
  }

  if (!Number.isFinite(num)) {
    return { ok: false, error: `Ошибка: ${name} не может быть бесконечностью` };
  }

  if (!Number.isInteger(num)) {
    return { ok: false, error: `Ошибка: ${name} должен быть целым числом` };
  }

  if (num < 0) {
    return { ok: false, error: `Ошибка: ${name} не может быть отрицательным` };
  }

  return { ok: true, value: num };
}

const planned = parseTaskInput(plannedText, "planned");
const completed = parseTaskInput(completedText, "completed");

if (!planned.ok) {
  console.log(planned.error);
} else if (!completed.ok) {
  console.log(completed.error);
} else if (completed.value > planned.value) {
  console.log("ошибка: completed не может превышать planned");
} else {
  const remaining = planned.value - completed.value;
  const percent = planned.value === 0 ? 100 : (completed.value / planned.value) * 100;

  console.log(`всего задач: ${planned.value}`);
  console.log(`выполнено: ${completed.value}`);
  console.log(`осталось: ${remaining}`);
  console.log(`процент выполнения: ${percent.toFixed(1)}%`);
}
￼
